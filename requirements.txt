# requirements.txt
# earth-systems-physics
# CC0 — No Rights Reserved

# Core physics
numpy>=1.24
scipy>=1.10

# API layer
flask>=2.3
flask-cors>=4.0

# Optional — needed only if running monitors.py standalone
# or if you want the background polling thread
# (already in stdlib: threading, collections, dataclasses, datetime)

# Dev / testing
pytest>=7.0
