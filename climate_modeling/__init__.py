# climate_modeling
# CC0 — No Rights Reserved
#
# AI-assisted modelling audit laboratory. Two layers:
#   Level 1 (experiments)      — run ecological models, measure aggregation bias.
#   Level 2 (meta-experiments) — audits where the true generative process is
#                                known, so any discrepancy from a simplified
#                                model is a genuine modelling failure; an AI
#                                proposer can iterate patches.
#
# Standalone sub-project. numpy + scipy only (no sklearn / streamlit).

__version__ = "0.1.0"
